- BUG - crafting manager references are stale if old tables are deleted

- refactor placement system with composition over inheritence
- have pickup behaviors follow checkbox config at pickup level
- refacor item slot code to handle events (runaway DRY causing ambiguous code)
